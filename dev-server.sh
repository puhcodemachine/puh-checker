#!/usr/bin/env bash
# Полный бэкенд для разработки/тестов: порт 8777, наружу закрыт фаерволом (ufw).
# Для проверки серверной версии (настоящий логин, API заданий, воркер) локально.
cd "$(dirname "$0")"
echo "локальный backend: http://127.0.0.1:8777  (наружу не открыт)"
PUH_PORT=8777 /root/PUH/.venv/bin/python server.py
